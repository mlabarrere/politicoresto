# Journal d'implémentation — PoliticoResto

## Environnement (2026-06-13)
- Conteneur : Node 22, pnpm, **Docker** (démarré manuellement via `dockerd`), Python 3.11, uv.
- `npm install` OK. **Baseline verte** : `npm run verify` (prettier+eslint+auth-guards+
  typecheck+**451 tests unit**) = exit 0.
- **Supabase local BLOQUÉ** : la politique réseau (egress allowlist) bloque le pull des
  images Docker Supabase (403 cloudfront ; « Host not in allowlist: eu.i.posthog.com »).
  CLI 2.106 installée (`~/.local/share/supabase`).
- **Modèle de vérification (plan B validé par Micky)** : code + migrations + tests ;
  `verify` local (sans Supabase) ; **push GitHub → la CI exécute intégration + E2E**
  (elle a son propre Supabase). Déploiement Vercel/Supabase quand tokens fournis.

## Audit schéma (clé)
- **`public.space` n'a jamais existé** (référence morte d'un pivot, cf. migration
  20260420220000). Donc modèle d'espaces = **greenfield**.
- Réutilise : `political_entity` (entités, hiérarchie via parent_entity_id), `app_profile`,
  `topic` (a déjà `space_id` vestigial, `entity_id`, `primary_territory_id`, `topic_territories`).
- Enums legacy `space_role/space_status` = résidu (alias de vues) — **non réutilisés**.
- `is_moderator()`, `citext` disponibles.

## Avancement P1
- [x] **Epic 1 / Story 1.1 — Modèle d'espaces unifié** :
      migration `20260613072000_space_model_unified.sql` (enums, `space`, `space_member`,
      `space_edge`, `topic_space`, helper `is_space_member`, RLS default-deny + policies,
      grants) + test d'intégration `tests/integration/space-model.int.test.ts`.
      **À valider par la CI** (db reset + test). Types TS à régénérer (besoin DB → CI/local).
- [ ] Story 1.2 (vues SECURITY INVOKER / advisors), 1.3 (i18n next-intl) — suivantes.

## Prochaines actions
1. Surveiller la CI sur le push (migration applique ? test RLS passe ?).
2. Si vert : Story 1.3 (i18n next-intl) ou 1.2.
3. Demander tokens Vercel/Supabase pour déploiement + vérif en ligne.

## MAJ 2026-06-14
- ✅ **Story 1.1 VALIDÉE EN CI** (commit 43856a1) : Integration + E2E + Build + Verify
  tous verts → la migration `space` s'applique et le test RLS passe. Durcie par 2
  relectures IA (Sourcery + Codex) : 5 bugs RLS/intégrité corrigés.
- ✅ **RPC `create_space`** (migration `20260614071900_space_create_rpc.sql`) : write-path
  atomique (espace + membership ; owner pour Table, membre pour Nœud ; verification
  reste 'unverified') + test d'intégration `space-create-rpc.int.test.ts`. Referme le
  point Codex (créateur d'une table privée = owner, peut la lire).
- Prochaine : Story 1.2 (vues SECURITY INVOKER) ou Epic 3 (graphe : page nœud/fiche SSR+JSON-LD).

## MAJ 2026-06-14 (P1 / Epic 3 — premier écran UI)
- ✅ **Page nœud `/n/[slug]`** (fiche d'entité, l'aimant SEO) : SSR + `generateMetadata`
  (OG) + **JSON-LD schema.org** (Organization/Person selon node_type) ; header App* +
  badges (Vérifié/Officiel) + section discussions (empty state). FR en dur (i18n = story
  ultérieure). Data-layer `lib/data/public/nodes.ts` (`getNodeBySlug`).
- ✅ **Amorçage du graphe** : migration `20260614120000_space_nodes_from_entities.sql`
  crée un Nœud (kind='node', verified) par parti/candidat de `political_entity` →
  graphe peuplé partout (CI+prod) + données E2E.
- ✅ **E2E** `node-page.spec.ts` : `/n/ps` (Parti socialiste) titre+badge+JSON-LD, 404.
  Verify local vert ; CI exécute migration + E2E.
- Prochain : lister les discussions rattachées au nœud (topic_space) + lien depuis le feed ;
  puis Boussole. UI à polir visuellement une fois la boucle visuelle débloquée (tokens).
